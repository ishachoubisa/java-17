package com.learn.BeyondClasses;

public interface WalksOnEightLegs {//does not compile if we make it final because the purpose of
                                   // interface is to be implemented by classes so that they can be used making it
                                   //  final we will not be able to used it.
}
