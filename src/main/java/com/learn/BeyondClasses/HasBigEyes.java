package com.learn.BeyondClasses;

public interface HasBigEyes extends Nocturnal,CanFly{//Extending two interface is permitted because interface is not instantiated
    //and unlike abstract classes they do not have constructors and are not of instance initialization.
}
