package com.learn.Structure;//Must be always the First statement in a class. If present(optional).

import java.util.*;//Must be the Second statement in a class, if present

public class Meerkat {//public class mendetory keyword, must be the third statement in a class
    double weight;//fields and methods can go in either order
    public double getWeight(){
        return weight;
    }
    double height;//another field - they don't need to be together.
}
